//! Enums Types

/*
//* 1) Numeric Enums (DEFAULT)
enum directions {
  up=10,
  down,
  left,
  right,
}

const moves:directions = directions.right;
console.log(moves)

//* 2) String Enums 
enum role{
    User="USER",
    Admin="ADMIN",
    Viewers="VIEWERS"
}

const type:role = role.Admin;
console.log(type)

//* 3) Heterogenous Enums
// In this type, we can mix both number and string in same Enum

enum direction {
    up=1,
    down="Hello",
    left=3,
    right="World!"
}

const move:direction = direction.left;
console.log(move)
*/